import type { SlideProps } from '../types';

// Slide 3 — Lessons Learned Getting Started
export const slide3: SlideProps = {
  title: "Lessons Learned Getting Started",
  content: [
    "Start small: 1 workflow, 3–5 nodes, typed state",
    "Guards & timeouts: stop runaway loops, enforce determinism",
    "Human-in-loop edges: confirm risky actions",
    "Structured logs: enable debugging & replay"
  ]
};
